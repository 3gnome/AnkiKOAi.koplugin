================================================================================
VOCABULARY CARD — Anki copy/paste templates for AnkiKOAi
================================================================================
Anki note type name:  Vocabulary Card
Plugin menu label:    Vocabulary Card (No AI)

Open this file in any text editor on your computer (Notepad, VS Code, etc.).
Copy each block below into Anki → Tools → Manage Note Types → Vocabulary Card → Cards.

Formatted guide with deck options:  docs/anki-vocabulary-card.md
On e-reader (View README):           docs/in-app/vocabulary-card.md

================================================================================
PART 1 — FIELDS (in this order)
================================================================================
  Phrase
  Definition
  Context
  Source

Delete default Front/Back. Keep one card type (Card 1).

================================================================================
PART 2 — FRONT TEMPLATE
(Cards → Card 1 → Front Template)
================================================================================

{{#Phrase}}
<div class="vocab-card vocab-card--front">
  <div class="phrase">{{Phrase}}</div>
  <div class="prompt">Recall the definition.</div>
</div>
{{/Phrase}}


================================================================================
PART 3 — BACK TEMPLATE
(Cards → Card 1 → Back Template)
================================================================================

{{#Phrase}}
<div class="vocab-card vocab-card--back">
  <div class="phrase phrase--back">{{Phrase}}</div>

  {{#Definition}}
  <div class="section">
    <div class="section-label">Definition</div>
    <div class="section-body" id="akoai-def">{{Definition}}</div>
  </div>
  {{/Definition}}

  {{#Context}}
  <div class="section">
    <div class="section-label">Passage</div>
    <div class="section-body context">{{Context}}</div>
  </div>
  {{/Context}}

  {{#Source}}<div class="source">{{Source}}</div>{{/Source}}
</div>

<script>
/* Style the part-of-speech labels (Noun, Verb, …) and grammar markers that
   dictionaries embed as plain text in the definition. Anki re-renders each
   card with a fresh DOM, so this runs cleanly every time. */
(function () {
  var el = document.getElementById("akoai-def");
  if (!el) return;
  var POS = ["Proper noun", "Noun", "Verb", "Adjective", "Adverb", "Pronoun",
             "Preposition", "Conjunction", "Interjection", "Determiner",
             "Numeral", "Article", "Particle", "Prefix", "Suffix",
             "Abbreviation", "Contraction", "Symbol", "Phrase"];
  var html = el.innerHTML;
  POS.forEach(function (p) {
    var pat = p.replace(/ /g, "\\s+");
    var re = new RegExp("(^|[\\s>.;:,])(" + pat + ")\\b", "g");
    html = html.replace(re, '$1<span class="pos">$2</span>');
  });
  html = html.replace(
    /\(\s*(transitive|intransitive|obsolete|archaic|historical|figurative|literal|formal|informal|dated|rare|colloquial|slang)\s*\)/gi,
    '<span class="usage">($1)</span>');
  el.innerHTML = html;
})();
</script>
{{/Phrase}}


================================================================================
PART 4 — STYLING
(Cards → Styling — shared by front and back)
================================================================================

.card {
  --bg: #ffffff;
  --paper: #ffffff;
  --ink: #1f2328;
  --muted: #6a737d;
  --hairline: #e1e4e8;
  --accent: #003a75;
  --pos: #7a3500;
  font-family: "Segoe UI", "Helvetica Neue", Arial, sans-serif;
  font-size: 18px;
  line-height: 1.5;
  color: var(--ink);
  background: var(--bg);
}

.card.nightMode {
  --bg: #16191d;
  --paper: #1b1f24;
  --ink: #e8eaed;
  --muted: #9aa0a6;
  --hairline: #2c3138;
  --accent: #6ea8fe;
  --pos: #e0a872;
}

.vocab-card {
  max-width: 44em;
  margin: 0 auto;
  padding: 0.5em 1.1em;
  text-align: left;
}

.vocab-card--front {
  text-align: center;
  padding-top: 2.2em;
}

.phrase {
  font-size: 1.7em;
  font-weight: 700;
  color: var(--accent);
  margin-bottom: 0.3em;
}

.phrase--back {
  font-size: 1.2em;
  text-align: left;
  border-bottom: 2px solid var(--accent);
  padding-bottom: 0.4em;
  margin-bottom: 0.7em;
}

.prompt {
  font-size: 0.78em;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
  margin-top: 2em;
}

.section {
  margin: 0.9em 0;
}

.section-label {
  font-size: 0.72em;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--muted);
  margin-bottom: 0.3em;
}

.section-body {
  text-align: left;
}

.context {
  font-size: 0.96em;
  font-style: italic;
  white-space: pre-line;
}

.source {
  font-size: 0.8em;
  color: var(--muted);
  margin-top: 1.6em;
  border-top: 1px solid var(--hairline);
  padding-top: 0.5em;
}

/* Part-of-speech labels (Noun, Verb, …): own line, small-caps sub-heading. */
.pos {
  display: block;
  margin-top: 0.8em;
  font-variant: small-caps;
  font-weight: 700;
  letter-spacing: 0.04em;
  color: var(--pos);
}
.section-body .pos:first-child {
  margin-top: 0;
}

/* Grammar/usage markers like (transitive), (obsolete). */
.usage {
  font-style: italic;
  color: var(--muted);
}


================================================================================
PART 5 — DECK (suggested)
================================================================================
Create deck:  English::Koreader

Plugin settings (on device):
  Settings → Card defaults → Vocabulary Card → Default deck
  Settings → Card defaults → Vocabulary Card → Note type: Vocabulary Card
  Optional: Preferred dictionary, One-tap send (Vocabulary)

Suggested deck-options preset:  Vocabulary Card (or reuse Wiki Card preset)

  New cards/day:              30–50
  Maximum reviews/day:        250
  Learning steps:             1m 10m 1d
  Graduating interval:        2 days
  Easy interval:              4 days
  Starting ease:              250%
  Bury related new/siblings:  OFF

Dictionary definitions are shorter than AI Wiki Cards — slightly higher daily
caps are often comfortable.

Full setup guide (fields, plugin settings, dictionary install):
  docs/anki-vocabulary-card.md
