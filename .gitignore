# Local config with API keys and LAN URLs (use configuration.lua.sample as template)
configuration.lua

# Plugin runtime data (stored on device under KOReader settings)
*_cards.json
*_settings.json
ankikooai_recent_sent.json

# Dev / emulator logs
koreader.log
koreader-emulator.log
*.log
debug-*.log

# Local Cursor / machine-specific dev context (see LOCAL_DEV.md.sample)
LOCAL_DEV.md
.cursor/

# KOReader per-book sidecar metadata (created when opening a test book)
*.sdr/

# Local test books
alice.epub
wizard-of-oz.epub

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
nul
debug_*.png
.vscode/
.idea/
*.swp
*~
